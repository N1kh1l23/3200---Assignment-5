# CS3200 Assignment 6 - MongoDB + Redis

This assignment uses Node.js to read tweet data from MongoDB and store/query results in Redis.

## Requirements

- MongoDB running on `localhost:27017`
- Redis running on `localhost:6379`
- Database: `ieeevisTweets`
- Collection: `tweets`

## Install dependencies

```bash
npm install